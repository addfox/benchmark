# Benchmark: Plasmo

TODO: Implement same extension as addfox (React + Tailwind 3, popup/sidepanel/devtools/options/space/background/content). Use same icons and i18n. Dev script: `plasmo dev`. Build output: `build/chrome-mv3-dev` or `build/chrome-mv3-prod`.
