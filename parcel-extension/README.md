# Benchmark: Parcel (official extension plugin)

TODO: Implement same extension with Parcel and official extension plugin (React + Tailwind 3). Use same icons and i18n. Dev script: `parcel watch manifest.json`. Build output: `dist`.
